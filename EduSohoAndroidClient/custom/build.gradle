apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion '21.1.1'

    defaultConfig {
        applicationId "com.edusoho"
        minSdkVersion 16
        targetSdkVersion 21
    }

    productFlavors {
        cuntom {
            applicationId "com.edusoho_edianshang.kuozhi"
        }
    }

    sourceSets {
        cuntom {
            manifest.srcFile 'AndroidManifest.xml'
            res.srcDirs = ['src/edusoho_edianshang/res']
            assets.srcDirs = ['src/edusoho_edianshang/assets'];
        }
    }

    //链接选项 移除lint检查的error
    android {
        lintOptions {
            abortOnError false
        }
    }

    //签名
    signingConfigs {
        releaseConfig {
            storeFile file("key/howzhi.jks")
            storePassword "100010000"
            keyAlias "cn"
            keyPassword "10001000"
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.releaseConfig
        }
    }
}

dependencies {
    compile project(':edusoho_v2')
    compile files('libs/android_api_3.6.jar')
}
