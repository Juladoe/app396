apply plugin: 'com.android.application'

android {
    compileSdkVersion 21
    buildToolsVersion '21.1.1'

    defaultConfig {
        applicationId "com.edusoho"
        minSdkVersion 9
        targetSdkVersion 21
    }

    productFlavors {
        {package} {
            applicationId "com.{package}.kuozhi"
        }
    }

    sourceSets {
        {package} {
            manifest.srcFile 'AndroidManifest.xml'
            res.srcDirs = ['src/{package}/res']
        }
    }

    //链接选项 移除lint检查的error
    android {
        lintOptions {
            abortOnError false
        }
    }

    //签名
    signingConfigs {
        releaseConfig{
            storeFile file("key/{package}.jks")
            storePassword "{storePassword}"
            keyAlias "cn"
            keyPassword "{keyPassword}"
        }
    }

    buildTypes{
        release {
            signingConfig  signingConfigs.releaseConfig
        }
    }
}

dependencies {
    compile project(':edusoho_v2')
}
