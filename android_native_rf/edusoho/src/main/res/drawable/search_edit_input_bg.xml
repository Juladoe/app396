<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <corners android:radius="5dp" />
    <stroke
	    android:width="1dp"
	    android:color="@color/search_edit_input_bg_stroke"
	    />
    <!-- shape的颜色，支持渐变 -->
    <gradient
        android:angle="270"
        android:centerColor="@color/search_edit_input_bg_gradient"
        android:centerY="0.45"
        android:endColor="@color/search_edit_input_bg_gradient"
        android:startColor="@color/search_edit_input_bg_gradient" />

</shape>