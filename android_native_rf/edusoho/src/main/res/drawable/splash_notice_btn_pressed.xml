<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 是否是圆角 -->
    <corners android:radius="5dip" />
    
    <stroke
	    android:width="1px"
	    android:color="@color/splash_notice_btn_stroke"
	    />
    <!-- shape的颜色，支持渐变 -->
    <gradient
        android:angle="270"
        android:centerColor="@color/splash_notice_btn_gtadient"
        android:centerY="0.45"
        android:endColor="@color/splash_notice_btn_gtadient"
        android:startColor="@color/splash_notice_btn_gtadient" />

</shape>